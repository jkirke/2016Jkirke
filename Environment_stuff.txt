apt -y install golang
export GOPATH=/home/ibmadmin
apt -y install git
apt -y install docker
apt -y install docker.io
mkdir -p $GOPATH/src/github.com/hyperledger
cd $GOPATH/src/github.com/hyperledger
git clone https://github.com/hyperledger/fabric.git
cd /tmp
git clone https://github.com/facebook/rocksdb.git
cd rocksdb/
make shared_lib && INSTALL_PATH=/usr make install-shared && ldconfig
cd /tmp
rm -rf /tmp/rocksdb
apt -y install python-setuptools
curl "https://bootstrap.pypa.io/get-pip.py" -o "get-pip.py"
python get-pip.py
pip install --upgrade pip
pip install behave nose docker-compose
pip install grpcio
pip install -I flask==0.10.1 python-dateutil==2.2 pytz==2014.3 pyyaml==3.10 couchdb==1.0 flask-cors==2.0.1 requests==2.4.3

curl -sL https://deb.nodesource.com/setup_6.x | sudo -E bash -
sudo apt-get install -y nodejs
sudo npm install npm -g
sudo aptitude install build-essential
sudo npm install -g hfc@0.5.0 --unsafe-perm
export GOPATH=/home/ibmadmin/gopath
mkdir -p $GOPATH/src/github.com/hyperledger/ 
cd $GOPATH/src/github.com/hyperledger/ 
git clone https://github.com/hyperledger/fabric.git 
cd fabric
git checkout v0.5-developer-preview 
git branch
git log -1
git checkout cfc20996a610af199a26dd601535fe5c7a2915fb 
curl https://storage.googleapis.com/golang/go1.6.3.linux-amd64.tar.gz -o go1.6.3.linux-amd64.tar.gz 
sudo tar -C /usr/local -xzf go1.6.3.linux-amd64.tar.gz 
export PATH=$PATH:/usr/local/go/bin
mkdir -p $GOPATH/src/github.com/chaincode_example02/ 
cd $GOPATH/src/github.com/chaincode_example02/
cp $GOPATH/src/github.com/hyperledger/fabric/examples/chaincode/go/chaincode_example02/*.go . 
mkdir -p vendor/github.com/hyperledger 
cd vendor/github.com/hyperledger 
git clone https://github.com/hyperledger/fabric 
curl https://blockchain-certs.mybluemix.net/us.blockchain.ibm.com.cert -o certificate.pem
curl https://blockchain-certs.mybluemix.net/zone.blockchain.ibm.com.cert -o zone.blockchain.ibm.com.cert
